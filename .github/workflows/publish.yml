name: '🚀 publish'

on:
  push:
    branch: main

jobs:
  release:
    name: 🚀 publish
    runs-on: ubuntu-latest
    steps:
      - name: 📚 checkout
        uses: actions/checkout@v3
      - name: 🟢 node
        uses: actions/setup-node@v3
        with:
          node-version: 14
          registry-url: https://registry.npmjs.org
      - name: 🚀 publish
        - run: npm install
        - run: npm test
        - uses: JS-DevTools/npm-publish@v1
          package_path: libs/docs-ui-vue
          with:
            token: ${{ secrets.NPM_AUTH_TOKEN }}
